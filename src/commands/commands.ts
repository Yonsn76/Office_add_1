/* global Office */
// Punto de entrada para comandos de la cinta (function commands).
// El add-in usa un boton ShowTaskpane, pero este archivo debe existir
// para el FunctionFile declarado en el manifest.

Office.onReady(() => {
  // Registrar aqui acciones de tipo ExecuteFunction si se agregan en el futuro.
});
